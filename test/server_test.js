var expect = require('chai').expect;
var Server = require(__dirname + '/../lib/server');

describe('the server', function() {
  it('should have a valid port', function() {
    expect(typeof(port)).to.eql('Number');
  });
});
